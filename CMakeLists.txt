cmake_minimum_required(VERSION 3.15)
project(CodeGenerationPOC
    VERSION 0.0.1
    LANGUAGES CXX
    )

# There's no proper cmake package yet to be used.
# Just forward to the example peoject:
add_subdirectory(example)
